# =============================================================================
# config.py — Глобальна конфігурація проєкту
# =============================================================================
# Цей файл містить константи та шляхи, що використовуються по всьому проєкту.
# Централізована конфігурація спрощує зміни і робить код більш читабельним.
# =============================================================================

from pathlib import Path

# ─── Шляхи ───────────────────────────────────────────────────────────────────

# Кореневий каталог проєкту — шукаємо батьківську папку, що містить "data/"
_HERE = Path(__file__).resolve().parent
PROJECT_ROOT = _HERE
while not (PROJECT_ROOT / "data").exists():
    PROJECT_ROOT = PROJECT_ROOT.parent

# Папка з CSV-файлами
DATA_DIR = PROJECT_ROOT / "data"

# ─── Назви файлів ─────────────────────────────────────────────────────────────

FILE_FOOD      = "wfp_food_prices_ukr.csv"
FILE_MARKETS   = "wfp_markets_ukr.csv"
FILE_EXCHANGE  = "exchange-rates_ukr.csv"
FILE_DEBT      = "external-debt_ukr.csv"

# ─── Параметри обмінного курсу ───────────────────────────────────────────────
# Ми використовуємо лише гривню (UAH) та серію "Local currency units per USD"
# (стандартне визначення МВФ: скільки гривень за 1 USD).
# "Standard local currency units per USD" — альтернативна серія, яку ігноруємо
# для консистентності аналізу.
EXCHANGE_CURRENCY = "UAH"
EXCHANGE_ELEMENT  = "Local currency units per USD"

# ─── Налаштування Dash ────────────────────────────────────────────────────────

APP_TITLE = "Аналіз цін в Україні"
APP_PORT  = 8055
DEBUG     = True

# ─── Стиль графіків ──────────────────────────────────────────────────────────

PLOTLY_TEMPLATE = "plotly_dark"
MAP_STYLE       = "carto-darkmatter"   # Темна карта без API-ключа

# ─── Колірна палітра для регіонів ────────────────────────────────────────────

COLOR_SCALE = "YlOrRd"   # Жовто-оранжево-червона шкала для цін
